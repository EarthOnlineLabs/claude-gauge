{
  "name": "claude-gauge-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ClaudeGauge landing page — DesignOnline (designonline-ui) skin",
  "scripts": {
    "prep:ds-css": "node scripts/strip-remote-fonts.mjs",
    "dev": "npm run prep:ds-css && vite",
    "build": "npm run prep:ds-css && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "designonline-ui": "^1.4.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "vite": "^5.4.21"
  }
}
